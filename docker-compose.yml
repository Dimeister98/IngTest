services:
  postgres:
    image: postgres:16
    container_name: ing-postgres
    environment:
      POSTGRES_DB: ing
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pwd
    ports:
      - "5432:5432"
    volumes:
      - store-data:/var/lib/postgresql/data

volumes:
  store-data: